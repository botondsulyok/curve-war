import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  // IMPORTANT: Change '/curve-war/' to your repository name
  base: '/curve-war/',
  build: {
    outDir: 'dist'
  }
})
